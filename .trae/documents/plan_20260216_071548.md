# Java UT Agent 继续开发计划 - TDD 方式

## Phase 1.2: 变异测试集成（进行中）

### 任务
1. 编写 `IterativeOptimizerMutationTest` 集成测试
2. 集成变异测试到 `IterativeOptimizer`
3. 在测试生成流程中加入变异得分评估

---

## Phase 2: 测试质量评估体系

### 2.1 测试质量评分器
- 编写 `TestQualityScorerTest` 测试用例
- 实现 `TestQualityScorer` 类
- 功能：综合评分（覆盖率 + 变异得分 + 可读性）

### 2.2 测试异味检测器
- 编写 `TestSmellDetectorTest` 测试用例
- 实现 `TestSmellDetector` 类
- 检测：重复测试、冗余断言、魔法数字、过长测试

---

## Phase 3: 测试自动维护

### 3.1 测试失效检测器
- 编写 `TestFailureDetectorTest` 测试用例
- 实现 `TestFailureDetector` 类
- 检测：编译失败、运行失败、超时

### 3.2 变更影响分析器
- 编写 `ChangeImpactAnalyzerTest` 测试用例
- 实现 `ChangeImpactAnalyzer` 类
- 分析代码变更对测试的影响

---

## 预期交付物

| 模块 | 测试用例数 | 核心类 |
|------|-----------|--------|
| 变异测试集成 | ~10 | IterativeOptimizer 扩展 |
| 质量评估 | ~25 | TestQualityScorer, TestSmellDetector |
| 自动维护 | ~20 | TestFailureDetector, ChangeImpactAnalyzer |

**总计：约 55+ 新测试用例，6+ 新核心类**